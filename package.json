{
  "name": "radio-gong",
  "version": "1.1.0",
  "description": "Alexa Skill for querying playlist from Radio Gong http://www.radiogong.com/",
  "repository": {
    "type": "git",
    "url": "https://github.com/sgruendel/alexa-radio-gong.git"
  },
  "main": "index.js",
  "scripts": {
    "invoke": "aws lambda invoke --function-name 'radioGong' --payload fileb://tests/events/alexa-start-session.json tests/out.txt && cat tests/out.txt",
    "test": "nyc --reporter=lcov --reporter=text mocha --timeout 10000",
    "deploy": "npm run zip && npm run upload",
    "upload": "aws lambda update-function-code --function-name 'radioGong' --zip-file fileb://index.zip",
    "zip": "npm prune --production && zip -r index.zip node_modules && cd src && zip ../index.zip *"
  },
  "author": "Stefan Gründel",
  "license": "GPL-3.0",
  "dependencies": {
    "alexa-sdk": "^1.0.25",
    "cheerio": "^1.0.0-rc.2"
  },
  "devDependencies": {
    "aws-lambda-mock-context": "^3.1.1",
    "chai": "^4.1.2",
    "eslint": "^4.18.0",
    "eslint-config-strongloop": "^2.1.0",
    "mocha": "^5.0.1",
    "nyc": "^11.4.1"
  },
  "eslintConfig": {
    "extends": "strongloop",
    "env": {
      "es6": true,
      "node": true,
      "mocha": true
    },
    "rules": {
      "indent": [
        "error",
        4
      ],
      "max-len": [
        "error",
        120,
        4,
        {
          "ignoreComments": true,
          "ignoreUrls": true,
          "ignoreStrings": true,
          "ignorePattern": "^\\s*var\\s.+=\\s*(require\\s*\\()|(/)"
        }
      ]
    }
  }
}
